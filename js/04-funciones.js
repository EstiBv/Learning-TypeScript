"use strict";
/* se declaran igual que en JS.
Recibe un parámetro (num), se indica el tipo de dato(:number) y el tipo de dato que devuelve ): string */
function getNumber(num) {
    return "Number is: " + num;
}
console.log(getNumber(2));
/* Aunque los datos no coincidan, se ejecuta pero en la terminar indica error*/
